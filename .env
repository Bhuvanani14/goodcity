# Environment Variables for Good City Application
# Replace these placeholder values with your actual configuration

# MongoDB Atlas Connection String
# Get this from MongoDB Atlas dashboard -> Clusters -> Connect -> Connect your application
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/good_city?retryWrites=true&w=majority

# JWT Secret Key
# Generate a secure random string for JWT token signing (at least 32 characters)
JWT_SECRET=your-secure-jwt-secret-key-change-this-in-production-32-chars-minimum

# Optional: Node Environment
NODE_ENV=development
